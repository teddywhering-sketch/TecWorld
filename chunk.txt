<h5 class="fw-bold mb-3 mt-4" style="color: #172033;">OS Abertas Recentes</h5>
<div class="card border-0 shadow-sm mb-4" style="border-radius: 20px;">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table align-middle mb-0 table-hover border-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-4 text-secondary fw-semibold">OS</th>
                        <th class="text-secondary fw-semibold">Cliente</th>
                        <th class="text-secondary fw-semibold">Tipo</th>
                        {% if is_operacional %}<th class="text-secondary fw-semibold">Técnico</th>{% endif %}
                        <th class="pe-4 text-secondary fw-semibold">Status</th>
                    </tr>
                </thead>
                <tbody>
                    {% for os in os_abertas_list %}
                    <tr>
                        <td class="ps-4"><a href="{% url 'ordem-detail' os.pk %}" class="fw-bold text-decoration-none" style="color: #4154f1;">{{ os }}</a></td>
                        <td class="fw-medium">{{ os.cliente }}</td>
                        <td><span class="badge text-bg-light border text-secondary">{{ os.tipo.nome }}</span></td>
                        {% if is_operacional %}<td class="text-secondary">{{ os.tecnico|default:'Não atribuído' }}</td>{% endif %}
                        <td class="pe-4"><span class="badge bg-primary rounded-pill px-3">{{ os.get_status_display }}</span></td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="5" class="text-center text-secondary py-4">Nenhuma OS aberta recente.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<h5 class="fw-bold mb-3 mt-4" style="color: #172033;">Aguardando Confirmação</h5>
<div class="card border-0 shadow-sm mb-4" style="border-radius: 20px;">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table align-middle mb-0 table-hover border-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-4 text-secondary fw-semibold">OS</th>
                        <th class="text-secondary fw-semibold">Cliente</th>
                        <th class="text-secondary fw-semibold">Tipo</th>
                        {% if is_operacional %}<th class="text-secondary fw-semibold">Técnico</th>{% endif %}
                        <th class="pe-4 text-secondary fw-semibold">Status</th>
                    </tr>
                </thead>
                <tbody>
                    {% for os in os_aguardando_list %}
                    <tr>
                        <td class="ps-4"><a href="{% url 'ordem-detail' os.pk %}" class="fw-bold text-decoration-none" style="color: #4154f1;">{{ os }}</a></td>
                        <td class="fw-medium">{{ os.cliente }}</td>
                        <td><span class="badge text-bg-light border text-secondary">{{ os.tipo.nome }}</span></td>
                        {% if is_operacional %}<td class="text-secondary">{{ os.tecnico|default:'Não atribuído' }}</td>{% endif %}
                        <td class="pe-4"><span class="badge bg-warning text-dark rounded-pill px-3">{{ os.get_status_display }}</span></td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="5" class="text-center text-secondary py-4">Nenhuma OS aguardando confirmação.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<h5 class="fw-bold mb-3 mt-4" style="color: #172033;">OS Finalizadas Recentes</h5>
<div class="card border-0 shadow-sm" style="border-radius: 20px;">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table align-middle mb-0 table-hover border-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-4 text-secondary fw-semibold">OS</th>
                        <th class="text-secondary fw-semibold">Cliente</th>
                        <th class="text-secondary fw-semibold">Tipo</th>
                        {% if is_operacional %}<th class="text-secondary fw-semibold">Técnico</th>{% endif %}
                        <th class="pe-4 text-secondary fw-semibold">Status</th>
                    </tr>
                </thead>
                <tbody>
                    {% for os in os_concluidas_list %}
                    <tr>
                        <td class="ps-4"><a href="{% url 'ordem-detail' os.pk %}" class="fw-bold text-decoration-none" style="color: #4154f1;">{{ os }}</a></td>
                        <td class="fw-medium">{{ os.cliente }}</td>
                        <td><span class="badge text-bg-light border text-secondary">{{ os.tipo.nome }}</span></td>
                        {% if is_operacional %}<td class="text-secondary">{{ os.tecnico|default:'Não atribuído' }}</td>{% endif %}
                        <td class="pe-4"><span class="badge bg-success rounded-pill px-3">{{ os.get_status_display }}</span></td>
                    </tr>
                    {% empty %}
                    <tr>
                        <td colspan="5" class="text-center text-secondary py-4">Nenhuma OS finalizada recente.</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
